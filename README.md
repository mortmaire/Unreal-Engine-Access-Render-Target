# Unreal-Engine-Access-Render-Target

My example to access the raw image data from render target in Unreal Engine. I noticed that there are no examples of code in UE4, so it's my attempt to fill this hole.
